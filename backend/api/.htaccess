RewriteEngine On

# Keep real files/directories working.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all API requests to api.php.
RewriteRule ^(.+)$ api.php [QSA,L]
